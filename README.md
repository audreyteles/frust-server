# frust-server

A server for uploading files

---

To execute this project, you have to run (after install Rust):

1. Change base path _(/your/path/here/)_:

```Rust
let path = format!("/your/path/here/{}", file_name);
```

2. Run server:

```
cargo run
```